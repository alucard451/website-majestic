---
import Content from "./text/Content.astro";
import Title from "./text/Title.astro";
import { links, styles, text } from "../utils/constants";
import Button from "./Button.astro";

// interface Props {
//   title: string;
//   content: string;
//   image: string;
// }

// const { title, content, image } = Astro.props;
---

<div class="flex justify-center">
  <div class="flex w-[1200px] h-[550px] items-center gap-6">
    <div class="flex flex-col w-3/5">
      <Title>{text.hero.title}</Title>
      <Content>{text.hero.content}</Content>
      <div class="pt-11">
        <Button
          text="Contáctanos"
          tail={styles.whatsapp}
          link={links.whatsapp}
        />
      </div>
    </div>
    <!-- <img src="" /> -->
    <div class="w-2/5 bg-slate-300 h-full">
      <p class="">a</p>
    </div>
  </div>
</div>
