export interface ButtonProps {
    text: string;
    link: string;
    style: ButtonStyles;
}

export interface ButtonStyles {
    whatsapp: "text-white bg-primary bg-[#59DD19] text-lg py-3 px-4 rounded-full "
}